generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Onibus {
  id               Int    @id @default(autoincrement())
  placa            String @unique
  identificacaoRFID String @unique
  modelo           String
  status            String
}